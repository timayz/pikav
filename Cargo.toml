[workspace]

members = [
    "./cmd",
    "./api",
    "./cluster",
    "./pikav",
    "./pikav-client",
    "./example"
]

[profile.release]
codegen-units = 1
panic = "abort"
lto = true
strip = true
