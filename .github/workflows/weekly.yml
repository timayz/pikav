name: Weekly

on:
  schedule:
    - cron: 0 9 * * 1

jobs:
  audit:
    uses: timayz/.github/.github/workflows/rust-audit.yml@v3.0.1

  renovate:
    uses: timayz/.github/.github/workflows/renovate.yml@v3.0.1
    secrets:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
